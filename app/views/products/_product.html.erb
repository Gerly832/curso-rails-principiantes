<div class="product">
<%= link_to product_path(product.id) do %>
<%= image_tag product.photo, width: 150 if product.photo.attached?%>

    <h2><%= product.title %></h2>
    <%= product.price %>
    <% end %>
</div>